# ============================================
# Secret - Sensitive Configuration
# ============================================
# NOTE: In production, use external secrets manager (Vault, AWS Secrets Manager, etc.)
# Base64 encoded values (echo -n 'value' | base64)
apiVersion: v1
kind: Secret
metadata:
  name: loja-veloz-secrets
  namespace: loja-veloz
  labels:
    app.kubernetes.io/name: loja-veloz
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  # API Key for gateway authentication
  API_KEY: "loja-veloz-prod-secret-key-2024"
  # Database credentials (if using PostgreSQL)
  # DATABASE_URL: "postgresql://user:password@postgres:5432/loja_veloz"
